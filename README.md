## Setup
After cloning the repository and entering root directory,
```
uv sync
```

## TODO
- currently, if the the CLF decay rate is too large, it drives the system velocity to zero before reaching the goal state. The lander is essentially hovering above the ground